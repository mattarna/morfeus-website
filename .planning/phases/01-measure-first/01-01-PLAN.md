---
phase: 01-measure-first
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/geo/measurement.md
  - docs/geo/prompt-panel.md
autonomous: true
requirements:
  - MEAS-01
  - MEAS-02
must_haves:
  truths:
    - "docs/geo/measurement.md exists and contains the GA4 custom channel group name, the exact referral-source regex, step-by-step UI instructions for creating the channel group in GA4, and a note that GTM container GTM-WPT8RFKZ already fires GA4 via consent-mode (no code change needed)"
    - "docs/geo/measurement.md lists exactly five LLM referral domains: chatgpt.com, perplexity.ai, gemini.google.com, copilot.microsoft.com, claude.ai"
    - "docs/geo/prompt-panel.md contains 15 numbered buyer questions (5 brand, 5 category, 5 problem) with IT text and EN translation for each"
    - "docs/geo/prompt-panel.md contains a baseline table with rows for ChatGPT, Perplexity, Gemini, Google AIO, Copilot, Claude and columns for Cited?/Position/Source URL/verbatim-extract"
    - "docs/geo/prompt-panel.md contains a documented monthly-repeat procedure (when to test, how to fill the table, what counts as improvement)"
  artifacts:
    - path: "docs/geo/measurement.md"
      provides: "GA4 LLM channel group spec + setup procedure"
      contains: "custom channel group"
    - path: "docs/geo/prompt-panel.md"
      provides: "Prompt panel template + baseline table + monthly procedure"
      contains: "brand / categoria / problema"
  key_links:
    - from: "docs/geo/measurement.md"
      to: "GA4 UI (accounts.google.com/analytics)"
      via: "step-by-step instructions referencing GTM container GTM-WPT8RFKZ"
      pattern: "GTM-WPT8RFKZ"
    - from: "docs/geo/prompt-panel.md"
      to: "ChatGPT, Perplexity, Gemini, Google AIO, Copilot, Claude"
      via: "baseline table rows"
      pattern: "ChatGPT|Perplexity|Gemini|Copilot|Claude"
---

<objective>
Create two committed reference documents that provide the measurement baseline for Phase 1:

1. docs/geo/measurement.md — GA4 LLM referral channel group specification + UI setup procedure. Documents exactly how to configure a custom channel group in GA4 that catches chatgpt.com, perplexity.ai, gemini.google.com, copilot.microsoft.com, and claude.ai as "AI Referral" instead of "Direct" or "Unassigned". No code change needed — GTM container GTM-WPT8RFKZ already fires GA4 via the existing Consent Mode v2 in src/app/layout.tsx.

2. docs/geo/prompt-panel.md — 15 buyer questions (IT+EN, 3 buckets: brand/category/problem) with a pre-filled baseline table per engine and a repeatable monthly procedure. The ICP is B2B: CEO/COO/CFO and Operating Partner/MARF roles at Italian SMEs undergoing AI adoption.

Purpose: These documents let Matteo (a) create the GA4 channel group with zero ambiguity, and (b) run the first prompt-panel baseline today and repeat it every month consistently.

Output: docs/geo/measurement.md, docs/geo/prompt-panel.md
</objective>

<execution_context>
@C:/Users/Matteo/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Matteo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/research/SUMMARY.md
@docs/seo-geo-master-plan.md
@src/app/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write docs/geo/measurement.md — GA4 LLM referral channel group spec</name>

<read_first> - src/app/layout.tsx — confirm GTM container ID (GTM-WPT8RFKZ) and Consent Mode v2 initialization - docs/seo-geo-master-plan.md — North-star KPI table (referral traffic row); FASE 0 GA4+GTM bullet - .planning/research/SUMMARY.md — PITFALLS section 10 (GEO measurement traps); Phase 1 description - .planning/REQUIREMENTS.md — MEAS-01 text (exact domains listed)
</read_first>

  <action>
    Create docs/geo/ directory if it does not exist. Write docs/geo/measurement.md with the following structure and content (prose directive — do not emit fenced code blocks here, write the actual document):

    Title: "LLM Referral Traffic — GA4 Custom Channel Group"

    Section 1 "Perché questo file" — one paragraph explaining that GA4 attributes LLM click-through traffic to Direct or Unassigned by default because LLM chat interfaces strip the Referer header or the referrer domain is not included in any default GA4 channel group. The fix is a Custom Channel Group definition. No GTM or codebase change is required — GTM-WPT8RFKZ already fires GA4 after consent via Consent Mode v2.

    Section 2 "Dominio di riferimento — i cinque LLM" — a Markdown table with columns: Engine | Dominio referral | Note. Rows:
    - ChatGPT | chatgpt.com | Include anche chatgpt.com/search (ChatGPT Search)
    - Perplexity | perplexity.ai | Motore di ricerca AI principale
    - Gemini | gemini.google.com | Google Gemini chat (non Google Search AIO)
    - Copilot | copilot.microsoft.com | Microsoft Copilot; si appoggia all'indice Bing
    - Claude | claude.ai | Anthropic Claude

    Section 3 "Custom Channel Group in GA4 — Setup step-by-step" — numbered list of exact GA4 UI steps:
    1. Vai su GA4 → Admin (icona ingranaggio in basso a sinistra) → Property column → Channel groups → Create new channel group.
    2. Nome del gruppo: "AI Referral Channels" (o "Canali AI Referral" per property IT).
    3. Clicca "Add channel" → nome canale: "AI Referral".
    4. Condition type: Session source. Operator: "matches regex". Value: chatgpt\.com|perplexity\.ai|gemini\.google\.com|copilot\.microsoft\.com|claude\.ai
    5. Clicca Save per il canale. Trascina il canale "AI Referral" in cima alla lista (alta priorità rispetto a "Direct").
    6. Clicca Save per il gruppo.
    7. Il canale diventa attivo per i dati futuri. I dati storici precedenti alla creazione restano in "Direct"/"Unassigned" — questo è il comportamento atteso di GA4.
    8. Verifica: dopo 24-48h di traffico reale, filtra il report Acquisition → Traffic Acquisition per il canale "AI Referral". Se vedi sessioni con source = chatgpt.com etc., il gruppo è attivo.

    Section 4 "Segmento di comparazione (opzionale)" — explain that a GA4 Exploration segment can be created in parallel (Explore → Segments → Session segment → Session source matches regex above) to back-apply the filter to historical data for ad-hoc analysis. This does NOT fix the channel group for default reports but is useful for one-off checks.

    Section 5 "Cadenza di monitoraggio" — bullet list: check weekly in the first month (zero sessions expected until content/visibility grows); monthly from month 2 onward; export screenshot to docs/geo/baselines/ each month; track trend in the prompt-panel baseline table.

    Section 6 "Note tecniche" — two bullets: (a) GTM container GTM-WPT8RFKZ gestisce il tag GA4; Consent Mode v2 è già inizializzato in src/app/layout.tsx — nessuna modifica al codice è necessaria per questa fase. (b) Non committare mai chiavi API o Measurement ID segreti — il GTM container ID e il Measurement ID GA4 sono identificatori client-side pubblici e non costituiscono un segreto.

  </action>

  <verify>
    <automated>grep -c "chatgpt" docs/geo/measurement.md && grep -c "GTM-WPT8RFKZ" docs/geo/measurement.md && grep -c "matches regex" docs/geo/measurement.md</automated>
  </verify>

<acceptance_criteria> - docs/geo/measurement.md exists - File contains the string "chatgpt\.com|perplexity\.ai|gemini\.google\.com|copilot\.microsoft\.com|claude\.ai" (the exact regex value) - File contains "GTM-WPT8RFKZ" - File contains all five engine rows in the domain table (chatgpt.com, perplexity.ai, gemini.google.com, copilot.microsoft.com, claude.ai) - File contains numbered setup steps referencing "Channel groups" in the GA4 Admin UI - File does NOT contain any secret keys, Measurement IDs, or service-account credentials
</acceptance_criteria>

<done>docs/geo/measurement.md committed; contains the five LLM domains, the exact regex, step-by-step GA4 UI procedure, and a note that no code change is needed.</done>
</task>

<task type="auto">
  <name>Task 2: Write docs/geo/prompt-panel.md — buyer question template + baseline table</name>

<read_first> - docs/seo-geo-master-plan.md — FASE 0 "Prompt panel baseline" bullet + example questions - .planning/REQUIREMENTS.md — MEAS-02 (12-15 domande buyer IT+EN, 3 bucket, baseline registrata, ripetibile mensilmente) - .planning/research/SUMMARY.md — Phase 1 description ("15 queries: 5 brand, 5 category, 5 problem — IT + EN"); PITFALLS section on GEO measurement traps (brand-query testing is a vanity metric; use category + problem queries)
</read_first>

  <action>
    Write docs/geo/prompt-panel.md with the following structure:

    Title: "Prompt Panel — Baseline LLM Visibility"

    Section 1 "Obiettivo" — two sentences: this template tracks whether Morfeus / Morf appears in LLM responses to the questions real buyers ask. Brand-only queries are a vanity metric; the signal that matters is category and problem queries where we are not already expected.

    Section 2 "Motori da testare" — bullet list: ChatGPT (chat.openai.com, no web-search plugin unless noted), Perplexity (perplexity.ai, default mode), Gemini (gemini.google.com), Google AIO (google.com — annotare se la query restituisce un AI Overview), Copilot (copilot.microsoft.com), Claude (claude.ai).

    Section 3 "Le 15 domande" — three subsections, each with five numbered questions in Italian, followed by the English translation on the next line (prefix EN:). Questions must be genuinely in the buyer's language — conversational phrasing, not keyword-stuffed. ICP is CEO/COO/CFO and Operating Partner/MARF at Italian SMEs.

    Bucket A — Brand (5 domande):
    1. Chi è Morfeus Hub? EN: Who is Morfeus Hub?
    2. Cosa fa Morf nel campo dell'intelligenza artificiale? EN: What does Morf do in the field of artificial intelligence?
    3. Chi è il fondatore di Morfeus Hub? EN: Who is the founder of Morfeus Hub?
    4. Morfeus Hub è affidabile per la consulenza AI? EN: Is Morfeus Hub reliable for AI consulting?
    5. Quali aziende hanno lavorato con Morfeus? EN: Which companies have worked with Morfeus?

    Bucket B — Categoria (5 domande):
    6. Migliori società di consulenza AI in Italia nel 2025 EN: Best AI consulting firms in Italy in 2025
    7. Chi offre formazione sull'intelligenza artificiale per le PMI italiane? EN: Who offers AI training for Italian SMEs?
    8. Come scegliere un partner per l'adozione dell'AI in azienda? EN: How do I choose a partner for AI adoption in a company?
    9. Agenzie AI per l'automazione dei processi aziendali Italia EN: AI agencies for business process automation Italy
    10. Consulenti AI per CEO e manager in Italia EN: AI consultants for CEOs and managers in Italy

    Bucket C — Problema (5 domande):
    11. Come integrare l'intelligenza artificiale nei processi aziendali senza perdere il controllo? EN: How to integrate AI into business processes without losing control?
    12. Come misurare il ROI dell'intelligenza artificiale in azienda? EN: How to measure the ROI of artificial intelligence in a company?
    13. Corso AI finanziato Regione Lombardia per dipendenti EN: Regione Lombardia AI training grant for employees
    14. Cos'è un AI Champion e come si forma in azienda? EN: What is an AI Champion and how do you develop one in a company?
    15. Come costruire una strategia di adozione dell'AI per una PMI italiana? EN: How to build an AI adoption strategy for an Italian SME?

    Section 4 "Tabella baseline" — a Markdown table with header row:
    | N° | Motore | Data test | Citato? (S/N) | Posizione nella risposta | URL fonte citata | Estratto verbatim (max 100 car.) | Note |

    Then 6 empty rows per question (one per engine) — so list questions 1-15 as groups, each with 6 blank rows. For readability, use a simplified layout: one table per bucket where the row key is "Q{N} — {Motore}". Provide rows for all 15 questions × 6 engines = 90 rows total. Each row starts blank (to be filled manually on the day of first test).

    Section 5 "Procedura mensile" — numbered steps:
    1. Aprire ogni motore in una sessione privata/incognito (evita personalizzazione).
    2. Incollare ogni domanda esattamente come scritta nel template; non aggiungere contesto.
    3. Registrare nella tabella: Citato? S/N; se S → posizione approssimativa nella risposta (inizio/metà/fine); URL fonte se visibile; estratto testuale (max 100 caratteri).
    4. Per Google AIO: cercare la query su google.com; annotare se compare un AI Overview e se Morfeus è citato.
    5. Salvare uno screenshot per ogni risposta positiva in docs/geo/baselines/YYYY-MM/.
    6. Aggiornare la riga "Trend" in fondo alla tabella con il totale citazioni per motore.
    7. Cadenza: prima settimana di ogni mese (≈ 90 minuti totali).
    8. Confronto: dopo 3 mesi, comparare il totale citazioni bucket B+C (categoria+problema) rispetto al mese 0. Il bucket A (brand) è informativo ma non è il KPI principale.

    Section 6 "Istruzioni per il bucket A" — short note: le domande brand servono a catturare uno snapshot iniziale (siamo già citati? con quale framing?). Non ottimizzare per queste. Se si è già citati nel bucket A ma non nel B+C, il problema è la superficie di contenuti, non la brand awareness.

  </action>

  <verify>
    <automated>grep -c "^[0-9]\+\." docs/geo/prompt-panel.md | head -1; grep -c "Perplexity" docs/geo/prompt-panel.md; grep -c "EN:" docs/geo/prompt-panel.md</automated>
  </verify>

<acceptance_criteria> - docs/geo/prompt-panel.md exists - File contains exactly 15 numbered questions (1–15) with both IT text and EN: translation - File contains all six engine names: ChatGPT, Perplexity, Gemini, Google AIO, Copilot, Claude - File contains a Markdown table with columns including "Citato?" and "URL fonte citata" - File contains the word "mensile" or "mensili" in the procedure section - File contains 3 bucket labels: Brand, Categoria, Problema - File does NOT contain any fake guarantee language (no "14 giorni", no "soddisfatti o rimborsati")
</acceptance_criteria>

<done>docs/geo/prompt-panel.md committed; contains 15 IT+EN buyer questions in 3 buckets, a baseline table for 6 engines, and the monthly repeat procedure.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary              | Description                                                           |
| --------------------- | --------------------------------------------------------------------- |
| Doc author → git repo | Executor writes Markdown files; no secrets, no API keys               |
| Analyst → GA4 UI      | Human follows doc instructions in GA4 console; no programmatic access |

## STRIDE Threat Register

| Threat ID | Category               | Component                  | Disposition | Mitigation Plan                                                                                                                                                       |
| --------- | ---------------------- | -------------------------- | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-01-01   | Information Disclosure | docs/geo/measurement.md    | accept      | GTM container ID and GA4 Measurement ID are client-side public identifiers already present in src/app/layout.tsx; committing them in a doc does not increase exposure |
| T-01-02   | Tampering              | prompt-panel baseline data | accept      | Baseline table is filled manually by Matteo; no automated write path; data integrity relies on process discipline, not technical control                              |
| T-01-SC   | Tampering              | npm/pip/cargo installs     | accept      | Phase 1 adds zero new npm dependencies; no install tasks in this plan                                                                                                 |

</threat_model>

<verification>
After both tasks complete:
- `ls docs/geo/` returns at minimum: measurement.md, prompt-panel.md
- `grep "chatgpt\.com|perplexity\.ai|gemini\.google\.com|copilot\.microsoft\.com|claude\.ai" docs/geo/measurement.md` returns the regex line
- `grep -c "EN:" docs/geo/prompt-panel.md` returns 15 (one EN translation per question)
- `git status docs/geo/` shows both files tracked (not untracked)
- `npm run typecheck && npm run lint && npm run build` passes (no code changes in this plan)
</verification>

<success_criteria>

- docs/geo/measurement.md: contains the five LLM domains, the exact session-source regex, numbered GA4 UI steps mentioning "Channel groups", and a note that GTM-WPT8RFKZ + Consent Mode v2 require no code change.
- docs/geo/prompt-panel.md: contains 15 numbered questions with IT text and EN translation, organized in Brand/Categoria/Problema buckets; includes a baseline table for 6 engines; includes a monthly procedure with cadence and comparison instructions.
- Both files committed to git on branch feat/seo-geo-architecture.
- No secrets or API keys appear in either document.
- CI sequence (lint → typecheck → test → build) passes unchanged.
  </success_criteria>

<output>
Create `.planning/phases/01-measure-first/01-01-SUMMARY.md` when done.
</output>
