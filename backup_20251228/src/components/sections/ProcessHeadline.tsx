"use client";

/**
 * Process Headline Section - Index 4
 * 
 * Simple transition section before the pinned process steps.
 * Large gradient headline only.
 */
export function ProcessHeadline() {
  return (
    <section className="relative h-screen w-full flex items-center justify-center bg-black">
      <h1 className="text-6xl md:text-8xl lg:text-9xl font-normal tracking-tighter text-center bg-clip-text text-transparent bg-gradient-to-b from-white to-[#E0CCFA]/70 leading-[0.9]">
        OUR PROCESS
      </h1>
    </section>
  );
}
