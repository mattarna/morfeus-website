# Copy Assets

Copy operativo gestito dentro `config.json` per questa prima versione del funnel.

Se servono varianti A/B:

1. duplicare le sezioni copy in questo folder;
2. aggiornare `config.json` mantenendo la promessa per asset coerente;
3. tracciare varianti in `funnelRegistry` (`abTest.enabled`).
